// 程序入口
var LayaAir3D = /** @class */ (function () {
    function LayaAir3D() {
        // Laya.TBPluginAdapter.init();
        // Laya.TBMiniAdapter.init();
        // Laya.HWMiniAdapter.init();
        // Laya.TTMiniAdapter.init();
        // Laya.BLMiniAdapter.init();
        // Laya.ALIMiniAdapter.init();
        // Laya.VVMiniAdapter.init();
        // Laya.QGMiniAdapter.init();
        // Laya.KGMiniAdapter.init();
        // Laya.BMiniAdapter.init();
        // Laya.QQMiniAdapter.init();
        // Laya.MiniAdpter.init();
        // //初始化引擎
        // Laya3D.init(0, 0, true);
        // //适配模式
        // Laya.stage.scaleMode = Laya.Stage.SCALE_FULL;
        // Laya.stage.screenMode = Laya.Stage.SCREEN_NONE;
        // //开启统计信息
        // Laya.Stat.show();
        // //添加3D场景
        // var scene: Laya.Scene = Laya.stage.addChild(new Laya.Scene()) as Laya.Scene;
        // //添加照相机
        // var camera: Laya.Camera = (scene.addChild(new Laya.Camera(0, 0.1, 100))) as Laya.Camera;
        // camera.transform.translate(new Laya.Vector3(0, 3, 3));
        // camera.transform.rotate(new Laya.Vector3(-30, 0, 0), true, false);
        // camera.clearColor = null;
        // //添加方向光
        // var directionLight: Laya.DirectionLight = scene.addChild(new Laya.DirectionLight()) as Laya.DirectionLight;
        // directionLight.color = new Laya.Vector3(0.6, 0.6, 0.6);
        // directionLight.direction = new Laya.Vector3(1, -1, 0);
        // //添加自定义模型
        // var box: Laya.MeshSprite3D = scene.addChild(new Laya.MeshSprite3D(new Laya.BoxMesh(1, 1, 1))) as Laya.MeshSprite3D;
        // box.transform.rotate(new Laya.Vector3(0, 45, 0), false, false);
        // var material: Laya.StandardMaterial = new Laya.StandardMaterial();
        // material.diffuseTexture = Laya.Texture2D.load("res/layabox.png");
        // box.meshRender.material = material;
        // Laya.TBPluginAdapter.init();
        // Laya.TBMiniAdapter.init();
        // Laya.HWMiniAdapter.init();
        // Laya.TTMiniAdapter.init();
        // Laya.BLMiniAdapter.init();
      
        Laya.ALIMiniAdapter.init();
      
        // Laya.VVMiniAdapter.init();
        // Laya.QGMiniAdapter.init();
        // Laya.KGMiniAdapter.init();
        // Laya.BMiniAdapter.init();
        // Laya.QQMiniAdapter.init();
        Laya.MiniAdpter.init();
       
        Laya3D.init(720, 1280, false, false, false);
        console.log("LayaAir3D2")
        var _self = this;
        Laya.stage.scaleMode = Laya.Stage.SCALE_FIXED_WIDTH;
        Laya.stage.screenMode = Laya.Stage.SCREEN_NONE;
        Laya.stage.alignH = Laya.Stage.ALIGN_CENTER;
        Laya.stage.alignV = Laya.Stage.ALIGN_MIDDLE;
        Laya.stage.frameRate = Laya.Stage.FRAME_SLOW;
        _self.m_loadLy = new Laya.Sprite();
        _self.m_loadLy.name = "_loadLayer";
        console.log("LayaAir3D3")
        Laya.stage.addChild(_self.m_loadLy);
        var w = Math.floor(Laya.Browser.width);
        var h = Math.floor(Laya.Browser.height);
        Laya.stage.width = 840;
        Laya.stage.height = (Math.floor(h / (w / 840)) + 1) & 0x7FFFFFFE;
        this.viewW = 720;
        this.viewH = Math.floor(h / (w / this.viewW));
        this.viewS = Laya.stage.width / this.viewW;
        this.m_loadLy.size(this.viewW, this.viewH);
        this.m_loadLy.scale(this.viewS, this.viewS, true);
        this.m_loadLy.graphics.drawRect(0, 0, this.viewW, this.viewH, "#00ff00");
        var label = new Laya.Label();
        label.text = "3339";
        label.fontSize = 50;
        label.centerX = label.centerY = 0;
        this.m_loadLy.addChild(label);
        console.log("stageWidth:", Laya.stage.width, Laya.stage.height, "this.viewW:", this.viewW, this.viewH, this.viewS);
    }
    return LayaAir3D;
}());
console.log("LayaAir3D")
new LayaAir3D();
//# sourceMappingURL=LayaAir3D.js.map